# 单位物编参考 (Unit)

## 🚫 强制禁令

| 禁止 | 正确做法 |
|------|----------|
| ❌ 直接读取模板 JSON 文件（如 `data_template/*.json`） | ✅ 使用脚本 `y3_obj_edit.py` 创建物编 |
| ❌ 随意填写模型 ID | ✅ **必须通过模型选择流程**（见下方「模型选择流程」章节） |

> ⚠️ **模型 ID 不是可以随便填的数字！** 必须先查询 `resources.csv` 或通过 MCP 获取官方模型列表，根据语义匹配后再使用。

---

## 🎯 单位类型与 ID 前缀

| 类型 | ID前缀 |
|------|--------|
| `近战小怪` | 2xxxxx |
| `远程小怪` | 2xxxxx |
| `近战精英` | 3xxxxx |
| `远程精英` | 3xxxxx |
| `近战boss` | 4xxxxx |
| `远程boss` | 4xxxxx |
| `近战英雄` | 1xxxxx |
| `远程英雄` | 1xxxxx |

---

## 📋 获取已有单位物编列表

**⚠️ 只能通过 MCP 获取物编值，禁止直接读取 JSON 文件！**

### MCP 工具

```
工具: y3editor.get_editor_unit_custom_data
参数:
  - 不传参数 → 返回所有自定义单位物编列表
  - id_list → 指定单位 ID 列表，如 [200001, 200002]
  - key_list → 指定属性字段列表，如 ["hp_max", "attack_phy"]
```

### 使用示例

```python
# 获取所有自定义单位
y3editor.get_editor_unit_custom_data()

# 获取指定单位的指定属性
y3editor.get_editor_unit_custom_data(
    id_list=[200001, 200002],
    key_list=["hp_max", "attack_phy", "model"]
)
```

---

## 🎯 模型选择流程

### 步骤 1：读取数据源

| 数据源 | 获取方式 | 说明 |
|--------|----------|------|
| 预设资源表 | 读取 `../excels/resources.csv` | 169 条预配置资源（含模型、头像、特效） |
| 官方模型库 | **MCP 工具获取** | 通过 `y3editor.get_official_editor_model` 获取完整模型列表 |

> ⚠️ **注意**：项目中不包含本地模型库文件，必须通过 MCP 获取模型信息！

### MCP 模型查询

```
工具: y3editor.get_official_editor_model
参数: 
  - 不传 ID → 返回完整模型列表（56000+ 条）
  - 传入 ID → 返回单个模型详情
返回: [{ "ID", "类型", "名称", "标签" }, ...]
```

**工作流程**：
1. 先在 `resources.csv` 中查找（优先使用预设配置）
2. 如果没有合适的 → 调用 MCP 获取官方模型列表
3. AI 从返回的模型列表中根据名称和标签进行语义匹配，选择最合适的模型

### 🔍 MCP 返回数据处理

MCP 返回的数据会保存到临时文件 `<agent>/cmtmp/` 目录下。由于数据量很大（56000+），需要使用脚本搜索：

### 步骤 2：匹配优先级

```
1. resources.csv 精确匹配 → 预设名称 == 单位名称
2. resources.csv 语义匹配 → 预设名称/标签 与单位描述语义相关
3. MCP 模型列表匹配      → 从官方模型库中按名称/标签语义选择
4. 兜底默认              → 使用默认模型 211073 （牛头战士）
```

### 步骤 3：AI 选择逻辑

AI 应该考虑：
- **名称语义**：「丛林蜘蛛」→ 找带「蜘蛛」「丛林」「森林」标签的模型
- **类型体型**：「Boss」→ 找体型较大、气势强的模型；「小怪」→ 普通体型
- **风格一致**：同一批单位尽量选同一美术风格（写实/卡通/国风）
- **⚠️ 避免重复**：同一批次生成的单位，尽量使用不同的模型

### 步骤 4：确认与生成

| 场景 | 行为 |
|------|------|
| 单个单位 | 直接生成，输出匹配理由 |
| 批量单位 | 先列出匹配结果表格让用户确认，确认后再生成 |

### 匹配结果输出格式

```markdown
| 单位名称 | 模型ID | 模型名称 | 匹配来源 | 匹配理由 |
|----------|--------|----------|----------|----------|
| 丛林蜘蛛 | 211064 | 蛛魔 | resources | 预设名称语义相近，远程小怪 |
| 火焰法师 | 211025 | 哥布林法师 | resources | 远程英雄，法师类型 |
```

> 💡 如果存在重复风险，在表格中标注 `⚠️ 重复` 提醒用户

---

## 🎯 击杀奖励字段（怪物必填）

| 字段名 | 用途 | 示例值 |
|--------|------|--------|
| `reward_exp` | 击杀经验奖励 | 小怪=10, 精英=50, Boss=200 |
| `reward_official_res_1` | 击杀金币奖励 | 小怪=5, 精英=30, Boss=100 |

**⚠️ 注意**：`dead_exp` 和 `dead_money` 不是击杀奖励字段！

| 单位类型 | 推荐 reward_exp | 推荐 reward_official_res_1 |
|----------|-----------------|---------------------------|
| 小怪 | 10-20 | 5-10 |
| 精英 | 50-80 | 25-50 |
| Boss | 150-300 | 80-150 |

---

## 🎁 死亡掉落物品配置

```json
// ✅ 正确格式 - 直接使用数组，不需要 __tuple__ 包装
"drop_items_tuple": [
    [510001, 1.0],    // 100% 掉落物品 510001
    [510002, 0.5],    // 50% 掉落物品 510002
    [510003, 0.3]     // 30% 掉落物品 510003
]
```

| 单位类型 | 推荐掉落概率 |
|----------|--------------|
| 小怪 | 0.1 - 0.3 |
| 精英 | 0.5 - 0.8 |
| Boss | 1.0 |

---

## 🎖️ 绑定技能到单位

```json
// 英雄技能（QWER）
"hero_ability_list": {
    "__tuple__": true,
    "items": [
        [100001001, 1],  // [技能ID, 技能槽位]
        [100001002, 2],
        [100001003, 3],
        [100001004, 4]
    ]
},
// 普通技能
"common_ability_list": {
    "__tuple__": true,
    "items": [
        [100001005, 1]
    ]
}
```

---

## 🚀 脚本命令

```bash
py -3 y3_obj_edit.py -map maps/EntryMap -type unit [选项] [参数]
```

---

## 📖 脚本参数说明

### 必选参数

| 参数名 | 说明 | 类型 | 示例 |
|--------|------|------|------|
| `-create` | 创建单位：单位ID,名称,类型,模型ID,头像ID | 整数,字符串,字符串,整数,整数 | `"200001,火焰蜘蛛,近战小怪,211064,903950"` |
| `-edit` | 编辑单位：单位ID | 整数 | `200001` |

### 可选属性参数

| 参数名 | 说明 | 类型 | 示例 |
|--------|------|------|------|
| `-name` | 名称 | 字符串 | `"火焰蜘蛛"` |
| `-description` | 描述 | 字符串 | `"喷吐火焰的蜘蛛"` |
| `-model` | 模型ID | 整数 | `211064` |
| `-icon` | 头像ID | 整数 | `903950` |
| `-hp_max` | 最大生命值 | 浮点数 | `500.0` |
| `-hp_max_grow` | 生命成长 | 浮点数 | `50.0` |
| `-mp_max` | 最大魔法值 | 浮点数 | `200.0` |
| `-mp_max_grow` | 魔法成长 | 浮点数 | `20.0` |
| `-attack_phy` | 物理攻击 | 浮点数 | `30.0` |
| `-attack_phy_grow` | 攻击成长 | 浮点数 | `3.0` |
| `-attack_mag` | 魔法攻击 | 浮点数 | `0.0` |
| `-defense_phy` | 物理防御 | 浮点数 | `5.0` |
| `-defense_mag` | 魔法防御 | 浮点数 | `3.0` |
| `-attack_speed` | 攻击速度（百分比） | 浮点数 | `100.0` |
| `-attack_interval` | 攻击间隔 | 浮点数 | `1.5` |
| `-common_atk_type` | 攻击类型（0=近战,1=远程） | 整数 | `0` |
| `-ori_speed` | 移动速度 | 浮点数 | `5.0` |
| `-turn_speed` | 转身速度 | 浮点数 | `10.0` |
| `-reward_exp` | 击杀经验奖励 | 浮点数 | `15.0` |
| `-reward_official_res_1` | 击杀金币奖励 | 浮点数 | `10.0` |
| `-hero_ability_list` | 英雄技能列表 | tuple 数组 | `"100001001:1,100001002:2"` |
| `-common_ability_list` | 普通技能列表 | tuple 数组 | `"100001005:1"` |

### 示例

```bash
# 创建单位
py -3 y3_obj_edit.py -map maps/EntryMap -type unit -create "火焰蜘蛛,近战小怪,211064,903950" -hp_max 500 -attack_phy 30 -reward_exp 15 -reward_official_res_1 10

# 编辑单位
py -3 y3_obj_edit.py -map maps/EntryMap -type unit -edit 200001 -hp_max 800 -attack_phy 50
```
